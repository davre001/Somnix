node_modules/
package-lock.json
yarn.lock
dist/
.env
.env.local
.env.*.local
# Throwaway testnet-only keypair generated for live lock/claim testing —
# no mainnet value, but never commit a private key regardless.
.testnet-wallet
.testnet-wallet-state.json
npm-debug.log*
*.tsbuildinfo
.claude/
.vercel
# Local Turso/libSQL fallback db (see frontend/src/lib/server/tursoStore.ts) —
# only used when TURSO_DATABASE_URL isn't set; real deploys use hosted Turso.
*.db
*.db-journal
*.db-wal
*.db-shm
